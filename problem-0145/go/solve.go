package main

import "fmt"

func main() {
	fmt.Println("Yo")
	Reverse(2)
}

/**
 * Reverses the digits of an integer. Returns am error if the number can't be reversed (ends with a 0 is the only failure
 * case I can think of)
 */
func Reverse(i int) (int, error) {
	return 1, nil
}
